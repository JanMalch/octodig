import { OpenedFilesMaxWidthPipe } from './opened-files-max-width.pipe';

describe('OpenedFilesMaxWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new OpenedFilesMaxWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
