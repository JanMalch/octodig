import { ItemInfoByShaPipe } from './item-info-by-sha.pipe';

describe('NameByShaPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemInfoByShaPipe();
    expect(pipe).toBeTruthy();
  });
});
