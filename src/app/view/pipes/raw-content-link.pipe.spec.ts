import { RawContentLinkPipe } from './raw-content-link.pipe';

describe('RawContentLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new RawContentLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
